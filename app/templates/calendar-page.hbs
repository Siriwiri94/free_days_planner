{{#if session.isAuthenticated}}
    {{#if currentUser.user}}
        <h1 class="title text-center">Calendar</h1>
            <div class="box">
        {{holidays-calendar}}
        </div>
    {{/if}}
{{/if}}
{{outlet}}