{{#if currentUser.user}}

    {{#link-to 'user_page'}}<a href="" class="button is-warning">{{fa-icon "arrow-left "}}&nbsp; Return</a>{{/link-to}}
    <div class="">{{reserve-form}}</div>
{{else}}
<h1 class="text-center text-danger font-italic"><u><i>You are not authorized</i></u></h1>
{{/if}}
{{outlet}}